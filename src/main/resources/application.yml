spring:
    application:
        name: API-Stack

server:
    session:
        cookie:
            http-only: true

management:
    context-path: /api-stack
    security:
        enabled: false
    health:
      db:
        enabled: false

security:
    ignored: /**
